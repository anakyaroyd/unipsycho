using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Cinemachine;

public class SDCamPrioritySwitcher : MonoBehaviour
{/*
    [SerializeField] CamSwitch _camSwitch;

    [SerializeField] CinemachineVirtualCamera _cmVC;

   // [SerializeField] CinemachineStateDrivenCamera _sdCam;

    [SerializeField] private int _normalPri;
    [SerializeField] private int _closePri;
    [SerializeField] private int _pausePri;


    private void Awake()
    {
      //  _sdCam = GetComponent<CinemachineStateDrivenCamera>();

        

    }

    // Update is called once per frame
    void Update()
    {
        _normalPri = _camSwitch.normalPri;
        _closePri = _camSwitch.closePri;
        _pausePri = _camSwitch.pausePri;

        _cmVC.m_Priority = _normalPri;
    }*/
}
